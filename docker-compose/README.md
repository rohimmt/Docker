# Ajax Web Login + Docker Compose
#### Build & Run:
  - ```$docker-compose build```
  - ```$docker-compose up```
  - Open phpmyadmin @localhost:8080 & import /html/db.sql
  - Open login web @localhost
#### Stop :
- ```docker-compose down```
